# File Pointer

A file pointer is used to access and manipulate files.

## Syntax

```c
FILE *ptr;
```

## Example

```c
FILE *ptr;
```

### Explanation

* `FILE` is a structure provided by `stdio.h`.
* `ptr` stores the address of the file.
* All file operations use a file pointer.
