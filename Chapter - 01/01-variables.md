# Variables

A variable is a container used to store data values in memory.

## Example

```c
int a = 3;
int b = 4;
```
Here:

* `a` stores the value `3`
* `b` stores the value `4`

## Why Variables?

Variables help us store and manipulate data during program execution.