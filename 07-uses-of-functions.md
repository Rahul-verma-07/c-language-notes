# Uses of Functions

Functions are useful because they:

* Avoid rewriting the same code.
* Improve readability.
* Make programs modular.
* Help in debugging.
* Allow code reuse.

## Example

Instead of writing the same logic multiple times, create a function and call it whenever needed.
